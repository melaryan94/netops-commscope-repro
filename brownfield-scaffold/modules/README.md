# Shared Terraform modules go here (optional).
#
# Extract a module only AFTER a few stacks are imported and you see real, repeated
# duplication — don't over-abstract up front. Example candidates: a standard
# "private web app + private endpoint" module, or a "hardened storage account" module.
